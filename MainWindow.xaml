<Window x:Class="Assignment11._3_ClientApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Assignment11._3_ClientApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="550">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Pet List" FontSize="30" FontFamily="Arial" FontWeight="Bold"/>
        <DataGrid Name="PetListDG" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id#" Binding="{Binding PetId}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding PetType}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding PetName}"/>
                <DataGridTextColumn Header="Breed" Binding="{Binding PetBreed}"/>
                <DataGridTextColumn Header="Birthdate" Binding="{Binding PetBirthDate}"/>
                <DataGridTextColumn Header="Color" Binding="{Binding PetColor}"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding PetGender}"/>
                <DataGridTemplateColumn Header="Delete">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeletePet"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Edit">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit" Click="SendToEdit"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>                
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Name="btnGetPetList" Grid.Column="0" Grid.RowSpan="2" Content="Get List" Click="GetPetList" Height="35" Width="60"/>
            <Button Name="btnSaveUpdate" Grid.Column="1" Grid.RowSpan="2" Click="SaveUpdate" Height="35" Width="70">
                <TextBlock Text="Save Update" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
            <Button Name="btnStartAddPet" Grid.Column="2" Grid.Row="0" Content="Add New" Click="StartAddPet" Height="20" Width="60"/>
            <Button Name="btnSaveAddPet" Grid.Column="2" Grid.Row="1" Content="Save New" Click="SaveAddPet" Height="20" Width="60"/>
        </Grid>
        <StackPanel Grid.Row="3" Margin="5">
            <Grid Margin="5" x:Name="PetInfoGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Name"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PetName}"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Type"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PetType}"/>
                <Label Grid.Row="2" Grid.Column="0" Content="Breed"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PetBreed}"/>
                <Label Grid.Row="0" Grid.Column="2" Content="Birthdate"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding PetBirthDate}"/>
                <Label Grid.Row="1" Grid.Column="2" Content="Color"/>
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding PetColor}"/>
                <Label Grid.Row="2" Grid.Column="2" Content="Gender"/>
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding PetGender}"/>
            </Grid>
                
        </StackPanel>
        
    </Grid>
</Window>
